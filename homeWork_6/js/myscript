//устранить ошибку с точкой запятой, строка 15 
//загружаю отдельным файлом в мой index.html

(function (win) {
	var params = {
		states: {url:"/",
		template: "temlate.js"}
		}
		function setStates(params) {
		if (win && !win.params) {
			win.params = params; }
			}
	setStates();
	console.log(params.states.template);
});
	(window);

// результат функции window
//  Window {postMessage: ƒ, blur: ƒ, focus: ƒ, close: ƒ,  frames: Window, …}